# The archiso hook is what mounts the squashfs the kernel was booted
# from. Without this file mkinitcpio builds an ordinary initramfs and the
# image does not boot at all. Taken verbatim from archiso's baseline
# profile - it is archiso's own configuration, not ours.
HOOKS=(base udev modconf archiso block filesystems)
