# Generic example profile - no specific hardware assumed.
# Real per-monitor layouts are detected via EDID (see Task 6) and written
# here at runtime; this fallback just accepts whatever is connected.
monitor=,preferred,auto,1
